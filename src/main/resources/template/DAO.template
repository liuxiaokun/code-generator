package com.cloudoer.${project}.${module}.dao;

import com.cloudoer.${project}.${module}.dao.BaseDAO;
import com.cloudoer.${project}.${module}.model.entity.${EntityName};
import org.apache.ibatis.annotations.Mapper;

/**
 * @author code gen
 * @version 0.0.1
 * @since 2018/11/20
 */
@Mapper
public interface ${EntityName}DAO extends BaseDAO<${EntityName}> {
}
